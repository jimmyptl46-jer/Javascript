const jimmy = {
  firstName : "Jimmy",
  lastName : "Patel",
  role : "Admin",
  courseCount : 3,

  getInfo : function(){
    console.log(`
   First name is ${this.firstName}
   Last name is ${this.lastName}
   His role is ${this.role}`);
  },
};

const dj ={
  firstName:"Rock",
  lastName:"DJ",
  role:"Sub-admin",
  courseCount:4,
}